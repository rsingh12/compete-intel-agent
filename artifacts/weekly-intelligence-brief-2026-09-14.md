## Weekly Brief: AI/ML Competitive Landscape
**Period:** 2026-09-07 to 2026-09-13 | **Author:** Competitive Intel Pipeline (gcp-analyst) | **Classification:** Internal

> Purpose: the recurring artifact that keeps leadership informed and field teams armed.

### Flash (Action Required)
- **Vertex AI unbundled memory/embedding token billing from the agent-compute rate, effective 2026-09-01, confirmed via this week's pricing-page capture.** The page now states verbatim: *"Tokens: model tokens used for memory generation and embeddings are billed separately under their respective model SKUs."* This is a cost-structure change, not a rate cut — a memory-heavy agent now stacks memory storage + read/write ops (billed as vCPU-h) **plus** embedding/generation tokens billed separately under the model SKU. The headline "$0.085/vCPU-h" Agent Compute figure a customer quotes is compute only; fully-loaded cost is higher. Firm and already live (past-dated 9/1) — usable in active bake-offs today. **AWS services pressured: Bedrock AgentCore (Memory, Runtime), Bedrock/Titan embeddings** — themes `agent_platform` + `ai_inference_price` — relevant at bake-off (agentic-app TCO) and renewal (bill-shock as usage scales). Ask any customer being pitched Vertex Agent Platform to model **fully-loaded cost per agent-session**, and bring an AgentCore worked example on the same basis.

### Priority Updates (This Week)
- **`gcp_pricing_vertex` (T1) was the only source to survive triage this week.** One prior-value note for completeness: no archived 9/7 rate can be diffed against for a % change, because the added lines announce net-new SKUs rather than a price move on an existing one — do not assert a delta that can't be verified.
- **`gcp_blog_ai` (T3) registered a byte-level diff; triage dropped every fragment as routine nav/rotation noise** — consistent with every prior week's pattern for this source. Not seller-relevant.
- **`gcp_pricing_compute` and `gcp_agentspace` fetched successfully and were byte-identical to last week** — no change, no signal on core compute pricing or the Agentspace product page.
- **Pipeline blindness now enters its 8th consecutive week.** `gcp_release_notes_all` (T1), `gcp_tpu_docs` (T2), and the SEC EDGAR 8-K index (`alphabet_earnings`, T1) again returned `403 Forbidden` on direct fetch. `PERPLEXITY_API_KEY` remains unset in this environment, so the documented fallback still cannot engage. Two of three blocked sources are Tier 1. Any GA notice, deprecation, or 8-K filed in the last eight weeks would have gone undetected.

### Strategic Signals (This Quarter)
- **Google keeps layering commit-discount and unbundling mechanics onto an agent-metering surface that is barely a month old.** The Agent Runtime per-vCPU/per-GiB meters and Memory Bank/Skill Registry billing went live 2026-09-01; the same pricing page now also splits out embedding/memory tokens from the compute rate. Read together with the 1yr/3yr Flexible Savings Plan (reported 2026-09-07), Google is monetizing the agent platform in pieces before it has much billing history — worth naming directly in any multi-year procurement conversation.
- **Semantic Governance Policy billing date remains internally inconsistent, now for a 5th straight week.** This week's capture again reads "billing on this pricing structure will commence later in 2026," identical language to the past three captures — still unreconciled against the 2026-08-10/08-17 briefs' "billing since Aug 1, 2026." Do not assert a Semantic Governance billing date to a customer in either direction until this is resolved against the live page.
- **Capex funding mix, TPU pricing (tpu7x $13.80/chip-hr, v6e $3.105/hr), and TPU supply/availability all remain unconfirmed for an 8th straight week** — all three depend on the same source outage blocking GA/deprecation and earnings visibility. Continue treating TPU throughput and general-availability claims as unverified.
- **The Oct 21, 2026 MaaS retirement (16 open-model endpoints: DeepSeek, Llama 3.3 70B, Qwen3 ×4, GLM, Kimi K2, MiniMax, GPT-OSS, E5 embeddings) is now just over 5 weeks out and unchanged.** Keep it as the anchor in active DeepSeek/Llama/Qwen bake-offs — the forced-migration window is shrinking, which raises its urgency in live conversations without any new information needed.

### Win/Loss Snapshot
- **Wins this week:** *no data source* | **Losses this week:** *no data source*
- **Top competitor in losses:** *no data source* | **Common theme:** *no data source*
- *Gap: this pipeline monitors public GCP surfaces only (pricing pages, release notes, product pages, earnings filings). It has no CRM/Salesforce feed, so it cannot populate deal counts, names, or themes — that data simply does not exist in this pipeline. Closing this gap requires wiring in an actual CRM export (a scheduled Salesforce or HubSpot report landing in this pipeline's collect stage) as a new, explicitly scoped source; nothing in this run added or should be read as approximating that data.*

### Recommended Actions
1. **Field:** Lead active Vertex/Agent Platform conversations with the fully-loaded cost point — memory/embedding tokens are now billed separately from the vCPU-h compute rate as of 9/1, so any customer quoting the "$0.085/vCPU-h" headline is quoting an incomplete number. Pair an AgentCore worked example on the same fully-loaded basis. Continue leading DeepSeek/Llama/Qwen bake-offs with the Oct 21, 2026 MaaS retirement list, now inside 6 weeks. Do not assert a Semantic Governance billing date to customers pending resolution of the 5-week-old page inconsistency.
2. **Pricing/Product:** No action needed on core compute list price — `gcp_pricing_compute` was byte-identical this week. This run's updated `state/` snapshots for `gcp_pricing_vertex` and `gcp_blog_ai` are committed with this brief so next week's run can compute a verified delta instead of restating current-state numbers.
3. **Pipeline/Ops — escalate, 8th week running, still no named owner:** `docs.cloud.google.com` and `www.sec.gov` remain 403'd at the outbound proxy, and `PERPLEXITY_API_KEY` is still unset so the pipeline's own documented fallback can't cover the gap. Either unblock the two domains at the network-policy layer or provision the Perplexity key — a real GA notice, deprecation, or 8-K filing may have been missed for two months, and there is still no way to check.
