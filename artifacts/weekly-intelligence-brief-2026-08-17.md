## Weekly Brief: AI/ML Competitive Landscape
**Period:** 2026-08-10 to 2026-08-16 | **Author:** Competitive Intel Pipeline (gcp-analyst) | **Classification:** Internal

> Purpose: the recurring artifact that keeps leadership informed and field teams armed.

### Flash (Action Required)
- **Standing item, now inside 2 weeks:** Vertex/Agent Platform metered billing for Memory Bank, Sessions, and Skill Registry turns on **September 1, 2026** (first flagged 2026-08-10). Combined with Agent Gateway (billing since Jul 13) and Semantic Governance (since Aug 1), every major agent-runtime primitive is now dated to bill within the quarter. Reps in active Vertex agent-platform discovery or bake-off should reframe prototyping-for-free as a closing budget event now, not at month-end.
- **Standing item, now inside 9 weeks:** Vertex AI's Oct 21, 2026 retirement of 16 open-model MaaS endpoints (DeepSeek, Llama 3.3 70B, Qwen3 ×4, GLM, Kimi K2, MiniMax, GPT-OSS, E5 embeddings) remains live and customer-checkable. Continue leading with it verbatim in any active DeepSeek/Llama/Qwen bake-off.
- **Pipeline blindness, now 4 consecutive weeks — escalate today, not another cycle:** `gcp_release_notes_all` (T1), `gcp_tpu_docs` (T2), and the SEC EDGAR 8-K index (`alphabet_earnings`, T1) again returned `403 Forbidden` this run, the same failure first reported 2026-07-27. Two of the three blocked sources are Tier 1. Any GA/deprecation notice or new 8-K filing has now gone unchecked for a full month. This run also confirms `PERPLEXITY_API_KEY` is unset in this environment, so the pipeline's own documented fallback (route blocked fetches through Perplexity) cannot engage — the outage has no working mitigation as configured today.

### Priority Updates (This Week)
- **Vertex AI / Agent Platform pricing re-verified against the prior capture (2026-08-12 → 2026-08-17), no confirmed price move.** Every list-price line item we can directly compare — H100 80GB ($9.797/hr + $1.469/hr management fee), H200 ($10.71/hr), tpu7x ($13.80/chip-hr), TPU v6e ($3.105/hr), Agent Compute ($0.085/vCPU-hr above the 50-hr free tier), Agent Memory ($0.009/GiB-hr), Agent Storage ($0.30/GiB-mo), and the full GCE/A2/A3/A4/G2/G4 machine-type tables — is byte-identical to the prior capture. No new discount, no new SKU, no rate change to quote.
- **Watch, not yet a confirmed change:** the same diff shows roughly 20 SKU groups (Vector Search reads/writes, Feature Store storage/serving nodes, ARIMA+ forecasting, AutoML Forecasting-on-Agent-Platform, Workbench compute/performance units, and several machine-type discount rows) that displayed an expanded "Show discount options" view with two additional committed-use price columns (~10%/20% off list) in the 2026-08-12 capture, and show only the single list price in this week's capture — with the underlying list price itself unchanged in every case (e.g., Performance-Optimized CU stayed $0.065/hr; only the $0.0585/$0.052 discount-tier columns dropped out). This reads as a page-render/default-view difference between two automated fetches rather than a deliberate CUD program change — we cannot confirm intent from a static diff, and it wasn't flagged as an isolated, dated commitment, so it does not belong in Flash. Do not tell a customer Google pulled committed-use discounts on these SKUs until a follow-up fetch confirms the columns stay gone.
- **Compute Engine pricing, Agentspace product page, and the AI/ML blog all changed bytes this run but carried no material content** — triage correctly dropped all three (a nav search-icon toggle on two pages, a routine blog-post rotation on the third). No field-facing signal.

### Strategic Signals (This Quarter)
- **Google is switching on metered billing across its entire agent runtime within a single quarter** — Agent Gateway (Jul 13) → Semantic Governance (Aug 1) → Memory Bank/Sessions/Skill Registry (Sep 1) — while wrapping the front door in simple per-seat SaaS pricing (Agentspace at $21–$30/seat/mo). Read together, it's a bundling play: monetize the plumbing, keep the headline price simple. AWS's answer stays TCO transparency (consumption pricing, no meter cliff) over feature-for-feature parity.
- **Capex funded externally, not just from free cash flow** ($49.6B stock issuance + $20.3B senior unsecured notes, per Q2 earnings) is still standing but now unconfirmed for a fourth straight week — the earnings source that would refresh it is one of the three sources currently 403'ing.
- **New TPU generation (tpu7x/8t/8i) pricing is public and unchanged this week** ($13.80/chip-hr), but supply/availability corroboration remains unverified for the fourth straight week — the TPU docs source that exists to confirm it is also part of the current outage. Do not repeat "3x throughput" or general-availability claims to customers as confirmed.
- **Model-serving consolidation continues on schedule:** the Oct 21 MaaS retirement and the earlier Gemini Flash sampling-parameter lockdown are unchanged this week — still reads as Google trading customer optionality for platform velocity.

### Win/Loss Snapshot
- **Wins this week:** *no data source* | **Losses this week:** *no data source*
- **Top competitor in losses:** *no data source* | **Common theme:** *no data source*
- *Gap: this pipeline monitors public GCP surfaces only (pricing pages, release notes, earnings filings). It has no CRM/Salesforce feed, so it cannot populate deal counts, names, or themes — that data simply does not exist in this pipeline. Closing this gap requires wiring in an actual CRM export (a scheduled Salesforce or HubSpot report landing in this pipeline's collect stage) as a new, explicitly scoped source; nothing in this run added or should be read as approximating that data.*

### Recommended Actions
1. **Field:** Lead active Vertex agent-platform discovery/bake-off conversations with the Sep 1, 2026 metered-billing cluster — it's inside two weeks and dated on the pricing page, not a projection. Keep leading DeepSeek/Llama/Qwen bake-offs with the Oct 21 MaaS retirement list.
2. **Pricing/Product:** Hold off on any claim that Google narrowed committed-use discounts on Vertex/Agent Platform SKUs — the base list prices didn't move, only a subset of discount-tier display columns dropped between two automated fetches. Confirm on the next run before this becomes field-facing material.
3. **Pipeline/Ops — escalate, fourth week running:** `docs.cloud.google.com` and `www.sec.gov` remain 403'd at the outbound proxy, and `PERPLEXITY_API_KEY` is unset so the pipeline's own documented fallback can't cover the gap. This needs a named owner this week — either unblock the two domains at the network-policy layer or provision the Perplexity key — before a real GA notice, deprecation, or 8-K filing is missed outright for a second month.
